const square = {
    area(side) {
      // returns value squared
      return side * side;
    },
    perimeter(side) {
      // value times 4
      return side * 4;
    },
};
// test
// square.area();
// square.perimeter();
